name: book_finder_app
description: A Flutter app for searching and saving books using Google Books API.
version: 1.0.0+1

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  http: ^0.13.5
  shared_preferences: ^2.2.0
  flutter_dotenv: ^5.0.2

flutter:
  uses-material-design: true