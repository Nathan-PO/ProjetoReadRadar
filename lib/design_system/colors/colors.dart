import 'package:flutter/material.dart';

class AppColors {
  // Cores primarias
  static const Color primary = Color(0xFF1E88E5); // azul
  static const Color primaryVariant = Color(0xFF1565C0);

  // Cores secundarias
  static const Color backgroundDark = Color(0xFF131117); // preto
  static const Color backgroundDarkVariant = Color(0xFF0A0A0E);

  // Cor de fundo
  static const Color background = Color(0xFFF5F7FA);
  static const Color surface = Color(0xFFF7F2FA);

  // Cores de textos e icones
  static const Color onPrimary = Colors.white;
  static const Color onBackgroundDark = Colors.black87;
  static const Color onBackground = Color(0xFF111827);
  static const Color onSurface = Color(0xFF111827);

  // Cor de alertas
  static const Color fav = Color(0xFFF54336);

  // Cores neutras
  static const Color disabled = Color(0xFFBDBDBD);
  static const Color divider = Color(0xFFE6E9EE);
}